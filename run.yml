name: Run show version command on Cisco devices
  gather_facts: no
  tasks:
    - name: Run show version command
      cisco.ios.ios_command:
        commands:
          - show version
      register: result

    - name: Display the output of show version
      debug:
        var: result
